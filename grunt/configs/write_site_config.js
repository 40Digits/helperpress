module.exports = {

	// defaults
	local: {
		options: {
			type: 'local'
		}
	},
	repo: {
		options: {
			type: 'repo'
		}
	},

	// target for prompt task
	repo_config: {
		options: {
			type: 'repo'
		}
	}
	
}